# Gradle
.gradle/
build/
*/build/
!gradle/wrapper/gradle-wrapper.jar

# Android Studio / IntelliJ
.idea/
*.iml
local.properties
.cxx/
.kotlin/

# Ad-hoc verification screenshots/scratch design files (not project deliverables)
/screen-*.png
/p2-*.png
/emulator-screenshot*.png
/logo_preview.svg
/logo_view.html
/logcat-dump.txt
/persist-log.txt
/*.db

# APK / AAB outputs outside releases/
*.apk
*.aab

# Release signing. The key and its passwords must never enter the repository: anyone with
# both can publish an update as this app, and losing the key means the app can never be
# updated again under the same identity.
keystore.properties
*.jks
*.keystore

# OS
.DS_Store
Thumbs.db
